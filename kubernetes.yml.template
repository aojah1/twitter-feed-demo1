apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: twitter-feed-demo1-v1
  labels:
    commit: ${WERCKER_GIT_COMMIT}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: twitter-feed-demo1
  template:
    metadata:
      labels:
        app: twitter-feed-demo1
        commit: ${WERCKER_GIT_COMMIT}
    spec:
      containers:
      - name: twitter-feed-demo1
        image: ${DOCKER_REPO}:${WERCKER_GIT_BRANCH}-${WERCKER_GIT_COMMIT}
        imagePullPolicy: Always
        ports:
        - name: twitter-feed-demo1
          containerPort: ${PORT}
          protocol: TCP
      imagePullSecrets:
        - name: wercker
---
apiVersion: v1
kind: Service
metadata:
  name: twitter-feed-demo1
  labels:
    app: twitter-feed-demo1
    commit: ${WERCKER_GIT_COMMIT}
spec:
  ports:
  - port: 30000
    targetPort: ${PORT}
  selector:
    app: twitter-feed-demo1
  type: ClusterIP
---
